name: Cypress Tests2

on: [push]

jobs:

 start:
    runs-on: ubuntu-latest
   
    steps:
    
    - name: Checkout Web
      uses: actions/checkout@v2.4.2
      with:
        path: ./frontend  
        
    - name: Web install npm
      run: | 
             cd frontend
             ls
             npm install
             npm start &


    - name: Checkout Cypress ${{ github.head_ref }}
      uses: actions/checkout@v2.4.2
      with:
       repository: rmhiguerey/cypress
       path: ./cypress
    
    - name: Cypress install npm 
      run: | 
             cd cypress
             ls
             npm install
             
             
    - name: Cypress.io
      uses: cypress-io/github-action@v2.9.7
      with:
       working-directory: /home/runner/work/app/app/cypress
       record: true
       browser: chrome
       
      env:
          CYPRESS_RECORD_KEY: bb5e29b2-35c0-4cef-a20d-5cf4b5257a27
          COMMIT_INFO_BRANCH: 123456
          COMMIT_INFO_MESSAGE: message 2
          COMMIT_INFO_AUTHOR: autor ronald
          COMMIT_INFO_SHA: hola sha
          COMMIT_INFO_REMOTE: 1223
