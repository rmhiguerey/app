name: Cypress Tests2

on: [push]
     
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
    
      - name: Checkout
        uses: actions/checkout@v2.4.2
        with:
          repository: rmhiguerey/app
      
      - name: Checkout
        uses: actions/checkout@main
        with: 
          repository: rmhiguerey/cypress
      
      - run: npm install
      - run: npm start
      - run: npm run cypress-e2e

     
